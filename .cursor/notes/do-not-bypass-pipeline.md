# Do not bypass a project's work pipeline

When a project has its own dispatch/worker pipeline (e.g. theOrg), "execute the plan" means setting up the scaffolding and creating tickets — not implementing the stories yourself. Let the pipeline pick up and execute the work unless the user explicitly asks for direct implementation.
