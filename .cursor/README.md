# Cursor artifacts

This directory contains Cursor-for-Cursor artifacts for this repo (rules, helpers, notes, skills, packs).

This repo also serves as **cursor-scaffold**: source-of-truth + tooling for Cursor project bootstrapping (packs are opt-in; projects stay clean).

